# ----------------------------------------------------------------------------
# Rust / Cargo
# ----------------------------------------------------------------------------
# ビルド生成物（これらは巨大かつ再生成可能なので絶対にコミットしません）
/target/

# Cargo.lock はアプリケーション（バイナリ）の場合はコミットするのが定石です
# ライブラリ開発の場合は除外することもありますが、今回はCMSなのでコミットします
# Cargo.lock

# ----------------------------------------------------------------------------
# Environment Variables & Secrets (最重要)
# ----------------------------------------------------------------------------
# DBパスワードなどが含まれるため、絶対にコミットしてはいけません
.env
.env.*

# ただし、変数のキー名だけを書いたサンプルファイルは共有用にコミットします
!.env.example

# ----------------------------------------------------------------------------
# Database (MySQL / SQLite / SQLx)
# ----------------------------------------------------------------------------
# もしSQLiteに戻った場合の一時ファイル
*.db
*.db-shm
*.db-wal

# SQLxのオフラインモード用データ（CI/CDでDBなしビルドをする場合に必要）
# 基本的にコミットすべきものなので、ここでは除外しません
# sqlx-data.json

# ----------------------------------------------------------------------------
# Frontend / Tailwind CSS / esbuild JS
# ----------------------------------------------------------------------------
# Tailwindのスタンドアロンバイナリ（サイズが大きいため除外）
tailwindcss
# esbuildのスタンドアロンバイナリ（サイズが大きいため除外）
esbuild

# 自動生成されるCSSファイル（ビルドプロセスで作れるため除外）
# ※デプロイ先でビルドしない運用なら、ここをコメントアウトしてコミットしてください
/public/css/style.css

# 万が一 npm/node_modules を使い始めた場合の保険
node_modules/
npm-debug.log
yarn-error.log

# ----------------------------------------------------------------------------
# IDE / Editor / OS
# ----------------------------------------------------------------------------
# VS Code
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.code-workspace

# JetBrains (IntelliJ / RustRover)
.idea/

# macOS / Windows
.DS_Store
Thumbs.db

/logs/